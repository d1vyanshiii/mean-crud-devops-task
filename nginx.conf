server {
    listen 80;

    # Serve frontend
    location / {
        proxy_pass http://frontend:80;
    }

    # Proxy API requests to backend
    location /api/ {
        proxy_pass http://backend:5000;
    }
}
